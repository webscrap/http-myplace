#!/usr/bin/perl -w
use strict;
use vars qw/$ModuleDir/;
use lib $ModuleDir;
use MyPlace::OddmuseMod;
use MyPlace::OddmuseMod::Debug;
use MyPlace::OddmuseMod::Utils;
use MyPlace::OddmuseMod::Hook;
use MyPlace::OddmuseMod::IO;
use MyPlace::OddmuseMod::Style;
